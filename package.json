{
  "name": "fanmodes",
  "private": true,
  "workspaces": ["apps/*", "services/*", "packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "prisma:migrate": "prisma migrate deploy --schema=prisma/schema.prisma"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  }
}
